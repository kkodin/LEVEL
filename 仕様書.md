# レベル野帳 (LEVEL) 開発仕様書 兼 引継ぎ書

最終更新: 2026-09-01 / 対応コミット: `6e23079` 以降

---

## 1. 概要

水準測量の野帳（**器高式**）をスマホで記録・計算するオフライン動作可能な PWA。
測量現場で片手操作できることを最優先に設計している。

| 項目 | 内容 |
|---|---|
| リモート | https://github.com/kkodin/LEVEL.git (branch `main`) |
| 作業リポジトリ | `C:\Users\OWNER\開発\LEVEL` ← **commit / push はここ** |
| バージョン保管庫 | `C:\Users\OWNER\Dropbox (個人用)\Documents\GITHUB\LEVEL\` |
| 技術 | 素の HTML / CSS / JavaScript。ビルド工程なし。外部ライブラリなし |

`app.js` は `<script src="app.js">`（非 module）で読み込むため**全関数がグローバル**。
ブラウザのコンソールから `chooseFs()` や `rows` を直接触って検証できる。

---

## 2. 版管理のルール（必須）

### フォルダ構成

```
C:\Users\OWNER\Dropbox (個人用)\Documents\GITHUB\LEVEL\
  LEVEL_vr014\          ← 最新版の完成状態（1つだけ直下に置く）
  OLD\                  ← 旧バージョンのフォルダを格納する
    LEVEL_vr000\ 〜 LEVEL_vr013\
```

### 手順

**大規模な改善・改修ごとに**以下を行う。小さな修正では版を切らず、作業リポジトリで直接コミットしてよい。

**スナップショットは「改修が終わった状態」を保存する。**
各 `LEVEL_vrNNN\index.html` をそのまま開けば、その版の完成品が動く状態にしておくこと。

1. 作業リポジトリ `C:\Users\OWNER\開発\LEVEL` で改修する
2. ブラウザで動作確認する
3. ユーザーに確認する
4. **ユーザーが明示的に「pushして」と言った時だけ** commit / `git push origin main` する
5. 直下にある既存の `LEVEL_vrNNN` を `OLD\` へ移動する
6. `LEVEL_vr(NNN+1)\` を直下に新規作成し、作業リポジトリの **`.git` を除く全ファイル**をコピーする
7. スナップショットに `VERSION.txt` を置き、対応コミットハッシュと変更概要を記録する

### 版番号

`LEVEL_vr000` から連番。直下には最新版だけを置き、それ以前は `OLD\` に入れる。

| 版 | 対応コミット | 場所 | 備考 |
|---|---|---|---|
| `LEVEL_vr000` | `dbd1e5b` | `OLD\` | 旧ルール（改修前の状態）で切った版 |
| `LEVEL_vr001` 〜 `LEVEL_vr009` | `ecaed8e` 〜 `acee0ab` | `OLD\` | 旧版 |
| `LEVEL_vr010` | `2cc7258` | `OLD\` | 表とテンキーの右端揃え・基準高／誤差の2列まで |
| `LEVEL_vr011` | `6e23079` | `OLD\` | 横向きのアコーディオン廃止・誤差バー廃止・測定情報の表示専用化・展開行の空欄修正まで |
| `LEVEL_vr012` | `c948a96` | `OLD\` | xlsx の表シートに 基準高・誤差mm 列と 日付/時刻/作業名 の3行を追加まで |
| `LEVEL_vr013` | `be651f0` | `OLD\` | 変位測量むけに「変位グラフ」シートの自動生成を追加まで |
| `LEVEL_vr014` | `4597134` | 直下（最新） | 完成状態。変位グラフに出す測点を登録済測点のチェックで選べるようにするまで |

### Dropbox 内で git 操作をしないこと

Dropbox 側は**バージョン保管庫専用**。ここに `.git` を置くと同期が `.git` 内のファイルを掴み、
`index.lock` 競合・競合コピーの混入・リポジトリ破損を招く。git 操作は必ず作業リポジトリで行う。

---

## 3. ファイル構成

| ファイル | 役割 |
|---------|------|
| `index.html` | 全画面のマークアップ（表・入力パネル・ドロワー・各シート・モーダル） |
| `app.js` | 全ロジック（約2700行の単一ファイル） |
| `styles.css` | 全スタイル |
| `service-worker.js` | オフラインキャッシュ |
| `manifest.json` | PWA設定 |
| `icon-180.png` / `icon.svg` | アプリアイコン（「Level / 野帳」2行デザイン） |
| `CLAUDE.md` | AI作業者向けの要点集（この仕様書の要約版） |

### Service Worker の更新（必須）

ファイルを変更したら**必ず**キャッシュ名を上げること。上げないと古いキャッシュが残り続ける。

```javascript
const VERSION = "0032";   // ← 番号を上げる
const CACHE_NAME = `level-book-vr${VERSION}`;
```

**同時に `index.html` の `?v=` も同じ番号に揃えること。**

```html
<link rel="stylesheet" href="styles.css?v=0032">
<script src="app.js?v=0032"></script>
```

キャッシュは2層ある。**Service Worker のキャッシュ名を上げるだけでは足りない。**
ブラウザのHTTPキャッシュは URL が変わらないと古いファイルを返し続けるため、
`?v=` でURL自体を変える。`service-worker.js` の `APP_FILES` も
`./app.js?v=${VERSION}` の形で同じURLを登録しておく（URLが違うとSWのキャッシュに当たらない）。

これを入れる前は、サーバが新しい内容を返しているのにブラウザが古い `app.js` を
使い続け、`Ctrl + Shift + R` を押すまで変更が反映されなかった。

---

## 4. データモデル

### 4.1 localStorage のキー

| キー | 内容 |
|---|---|
| `levelBook.tables.v1` | 表（計算表）の配列。実質的な本体 |
| `levelBook.savedPoints.v1` | 登録済み基準点 `[{name, value, chart?}]`。`chart` は変位グラフに出すかの指定で、**未設定なら自動判定**（→ 第4.2.2章） |
| `levelBook.meta.v1` | 現場情報 `{title, site, date, place, limit?, warnRatio?}`。`limit` は管理値(mm)、`warnRatio` は警告値の割合。**どちらも未設定なら既定値 45 / 0.8** |
| `levelBook.image2.v1` | 旧形式の行データ（後方互換のため残置） |

### 4.2 表と行

```javascript
tables = [ { name: "表1", date: "2026-08-30", time: "09:05", rows: [...] }, ... ]
rows   = [ { bs, ih, fs, gl, point, isBase }, ... ]   // isBase 以外は文字列
```

`ih` と `gl`（行1以降）は `calculate()` が毎回算出する**計算列**。手入力はできない。

`isBase` は「その行の GL が計算結果ではなく、**新たに置き直した基準高**である」ことの印。
行0 は常に基準行なので印を見ない（`isBaseRow(row)` が `row === 0` を特別扱いする）。
基準高を明示的に置く4経路（`applyBasePointSelection` / `recallPoint` /
`applyBaseEntry` / `applyDrawerPointName`）で `isBase: true` を立てる。

`time` は作成時刻。24時制の `"HH:MM"`（`normalizeTimeInput()` が `"H:MM"` や `"HHMM"` も受け付ける）。
**表を新しく作る経路では必ず `nowTimeString()` を入れる**
（`addTable` / `finishSetupAndChooseBasePoint` / `startNewSite` の3か所）。
一方、`normalizeTable()` や取り込みの fallback では空のままにして、
時刻を持たない古いデータに現在時刻を後付けしない。
日付と同じく**表追加ダイアログと表の名称変更ダイアログの2か所でだけ**編集できる（2026-09-01 に変更）。
測定情報の欄は表示専用になったので、そこからは変えられない（→ 第5.1章）。
表示は「計算表」の一覧と画面上部の要約に、時刻があるときだけ日付の後ろに付ける。

xlsx のシート名は `YYYY.MM.DD_HHMM_名前`（時刻が無ければ従来どおり `YYYY.MM.DD_名前`）。
シート名は31文字で切れるうえ記号が使えないので、**表シートの先頭3行にも
`日付` / `時刻` / `作業名` を書き出す**（`tableSheetRows`）。読込は
`tableMetaFromSheet()` がこの3行を優先し、無ければ `tableMetaFromSheetName()` が
シート名から復元するので、古いファイルもそのまま取り込める。
CSV取り込みは `TABLE,名前,日付,時刻` の4列目を見る（無ければ空）。

### 4.2.1 xlsx の表シートの形

```
A          B      C     D     E       F       G
1  日付      2026.09.02
2  時刻      09:30
3  作業名    1号線
4  BS       IH    FS    GL    測点名   基準高   誤差mm   ← 見出し
5  1.148    =…    　    10.000 KBM     =…      =…       ← データ1行目
```

- 行番号は `XLSX_TABLE_HEADER_ROW`(=4) / `XLSX_TABLE_FIRST_DATA_ROW`(=5) から計算する。
  **`xlsxMeasurementRow()` に行番号を直書きしないこと**
- `基準高` は `VLOOKUP` で基本情報シートの基準点一覧（`XLSX_POINTS_RANGE`）を引く。
  `basicSheetRows()` の行数を変えたら `XLSX_POINTS_ROW` も直すこと
- `誤差mm` は `ROUND((GL-基準高)*1000,0)`。表示は `+0;-0;+0` 形式（styles の numFmt 165）
- `基準高`・`誤差mm` は確認用の計算列。**読込側（`parseXlsx` / `parseExcelXml`）は A〜E しか見ない**
- 見出し行は `tableHeaderIndex()`（A列が `BS` の行）で探すので、
  見出しが1行目だった旧形式のファイルも読める

### 4.2.2 変位グラフシート（変位測量）

同じ測点を日をまたいで測り、初期値からの沈下・変位を追う使い方に対応する。

**考え方**: 初期値を基準点として登録すれば、`誤差mm` 列がそのまま変位になる。
測量ロジックは足していない。足りないのは「回ごとに横へ並べ直す集計」だけ。

```
    A            B                  C             D          …
1  作業名        路面変異測量初期値   路面変異測量   路面変異測量
2  日時          8/25 21:30         8/25 22:25    8/26 21:20
3  G1            +0                 +3            +5
4  G2            +0                 +3            +3
5  G3            +0                 +1            +2
6  管理値 (mm)   =基本情報!$B$6      …
7  警告値 (mm)   =基本情報!$B$6*$B$7 …
```

| 決めごと | 中身 |
|---|---|
| 値の引き方 | `INDEX`+`MATCH` で**測点名から**引く。行番号を直接指さない |
| 列の順 | 日付＋時刻の昇順。日付が無いものは元の並びのまま後ろへ |
| 行に出す測点 | 登録済測点の「☑ 変位」で測点ごとに指定。未設定なら自動判定で「行0の器械据付基準（KBM 等）を除く」 |
| 作らない条件 | 表が1つだけ／対象測点なし |
| 管理値・警告値 | 基本情報シートの `LIMIT`（mm）と `WARN`（割合）。保存ダイアログで変えられる |

行番号でなく測点名で引くのは、**Excel 側で不要な測点や行を消しても壊れないようにするため**。
実際、手作業で作られた同種のファイルには行削除による `#REF!` が残っていた。

グラフそのものの埋め込み（`xl/charts/`）はまだ入れていない。
この表の 日時 行＋測点行＋管理値／警告値 行を選んで折れ線を挿入すれば同じ図になる。

**集計シートを足したときは `isSummarySheetName()` にも足すこと。**
読込側が野帳の表として取り込んでしまう。

#### 変位グラフに出す測点の選び方

`isChartPoint()` が決める。

| `point.chart` | 動き |
|---|---|
| `true` / `false` | 測点ごとの明示指定。登録済測点の「☑ 変位」で書き換わる |
| `undefined`（未設定） | 自動判定。行0の器械据付基準（KBM 等。常に変位0）を外す |

**`chart` に既定値を入れないこと。** 「未設定＝自動」でないと、基準点を後から
足したときに勝手に外れる／入る。基準点の値を上書きしても（`registerSavedPoint`）
`chart` は保たれる。

指定は 基本情報シートの C列（`○` / `×`）に書き出して読み戻す。
C列が無い古いファイルは未設定＝自動として読む。

UI は登録済測点（`#pointList`）の各行のチップ。
**`.point-item` は `<button>` なので入れ子の `<input type="checkbox">` は置けない。**
`span.point-chart` にして、行の click ハンドラで
`event.target.closest(".point-chart")` を見てチップと行タップを振り分けている。

### 4.2.3 基本情報シートの行番号

`basicSheetRows()` が書き出す。A列のタグが読込側の目印で、C列は人が読むための説明。

| 行 | A列（タグ） | B列 | C列 |
|---|---|---|---|
| 1 | `LEVEL_APP` | `4`（書式のバージョン） | |
| 2 | `TITLE` | 読み込んだファイル名 | |
| 3 | `SITE` | 現場名（＝保存ファイル名） | |
| 4 | `DATE` | 現場の日付 | |
| 5 | `PLACE` | 作業名 | |
| 6 | `LIMIT` | 管理値 (mm)。既定 `45` | `管理値 (mm)` |
| 7 | `WARN` | 警告値の割合。既定 `0.8`（0% 書式で `80%` と表示） | `警告値の割合` |
| 8 | | 空行 | |
| 9 | `POINTS` | `初期値`（表示名） | |
| 10 | `測点名` | `数値` | `変位グラフ` |
| 11〜 | 測点名 | 値 | `○`＝変位グラフに出す / `×`＝出さない（空欄なら自動判定） |

**行を増減したら `XLSX_LIMIT_ROW` / `XLSX_WARN_ROW` / `XLSX_POINTS_ROW` を必ず直すこと。**
表シートの `基準高` 列の `VLOOKUP` 範囲（`XLSX_POINTS_RANGE`）と、
変位グラフシートの管理値・警告値の参照が、この定数から組み立てられている。
**読込は A列のタグを見ているので行がずれても読めるが、書き出した数式のほうが壊れる。**

`POINTS` は `初期値` と書き換えられたファイルも section の始まりとして読む。
変位測量では「初期値」と呼ぶため、手作業で書き換えられていた実例がある。

### 4.3 器高式の計算（最重要）

**同じ行の FS と BS は同一の移器点を指す。**

```
行0: GL=10.000  BS=1.500            → IH=11.500     ← 基準点の行
行1: FS=0.500   BS=2.000            → GL=11.000, IH=13.000
行2: FS=1.000                       → GL=12.000     ← BSなし = 中間視(IP)
```

`calculate()` の中身:

```javascript
// 基準行 (index===0 または isBase)  : GL は手で置いた値をそのまま使い、器高を計算し直す
// それ以外で FS があるとき          : GL_i = 直前のIH - FS_i
// それ以外で FS が無いとき          : GL_i を空にする（前回の計算結果を残さない）
// GL と BS が揃ったとき             : IH_i = GL_i + BS_i
```

**行0の FS は計算に使われない**ので、**選択もできない**（`isUnusedCell()`）。
セルには斜線の背景（`td.unused`）を付けて、使わないセルだと分かるようにしてある。
BS を持たない行（中間視）は、次の行に FS があるときだけ IH 欄に器高を表示する。

### FS / BS を消したときの挙動

**FS を消したら、その FS から求めた GL・IH も消える。**
以前は FS を消しても GL が残り、その古い GL から器高を計算し続ける不具合があった。
`calculate()` の「FS が無い非基準行は `next.gl = ""`」がこれを防いでいる。

BS を消した場合は、その行が中間視になるだけなので、上位から引き継いだ器高が IH 欄に出る
（これは古い計算結果ではなく、計算し直した正しい値）。

### BS を入力してよい行の条件（この章の要）

```
FS に数値がある行 ＝ GL（求めたい地盤高）が分かっている行
        ↓
その行に BS を入れると IH（機械高）= GL + BS が求められる
        ↓
＝ BS に数値を入れる意味がある
```

裏を返すと、**FS が空で基準行でもない行は GL が求まっていないので、
BS を入れても IH を出せない＝入力しても意味がない**。
そのため BS セルへ入る経路すべてで、先に基準高を決めてもらうか、操作自体を止める。

| BSセルへ入る経路 | GLが求まっていない行に対する扱い |
|---|---|
| BS ボタン | FS列に数値が1つも無ければ**ボタンを無効化**。あれば移動先で基準高を求める |
| ↑↓・矢印キー | `requestNewBaseGl()` で基準高を求める。選ばなければ元のセルへ戻す |
| セルを直接タップ | 同上 |

このとき出すのは既存の**「基準点の設定」シート**（`openBasePointSheet(row, false)`）。
登録済みの基準点から選ぶことも、測点名＋標高GLを新規入力して追加することもできる。

> **決定済み（2026-08-30）**: この画面で入力した点は**基準点として恒久的に登録**され、
> 閉合差チェックの照合対象にもなる。これは意図した仕様であり、
> 「その行のGLだけ入れて登録はしない」簡易ダイアログは作らない。

### 基準高の置き直し（計算のやり直し）

FS が空の行に BS を入れたい場合、その行には器高を求める元になる GL が無い。
そこで**新たに基準高を置いて計算をやり直す**必要がある。
`isBase` の立った行では `currentIH` を `null` に戻してから計算を再開する。

---

## 4.4 表の見出し行を固定する

行が増えて `.table-wrap`（`max-height: 292px; overflow: auto`）がスクロールしても、
見出し行が常に見えるように `thead th` を `position: sticky; top: 0` にしてある。

**`.level-table` は `border-collapse: separate; border-spacing: 0` にすること。**
`collapse` のままだと罫線がセルではなく表に属するため、見出しを sticky にしたとき
下の行が見出しの背景を透けて見えてしまう。
そのぶん罫線は各セルの `border-right` / `border-bottom` で引き、**最終列だけ**消している。
**最終行の下罫線は消さないこと**（2026-09-01）。表の終わりが分からなくなるため。
`.table-wrap` の外枠は 2px の濃色なので、1px の細線と重なっても二重線には見えない。

---

## 5.1 測定情報は「表示専用」（2026-09-01）

作業中に触って壊さないよう、測定情報の4項目は**普段どこからも編集できない**。
`updateMetaEditable()` が該当の `<input>` を `disabled` にしている。
変えられるのは次の場面だけ。

| 項目 | 実体 | 変えられる場所 |
|---|---|---|
| 現場名（ファイル名） | `meta.site` | **ファイルを保存するとき**（`exportExcel` のダイアログ） |
| 作業名（表の名前） | `table.name` | **表を追加するとき / 表の名称を変更するとき**（`addTable` / `renameTable` のダイアログ） |
| 作成日 | `table.date` | 同上 |
| 作成時刻 | `table.time` | 同上 |

**例外は新規現場の初期設定だけ。** ドロワーの `setup` モードでは 4項目とも入力できる。
ここが唯一「最初に決める」場所なので、閉じると `setupComplete` が入力を要求する。

- `date` / `time` は `readOnly` だとブラウザのピッカーから変えられてしまうので **`disabled`** にする
- `disabled` の入力欄はブラウザ既定だと文字が薄くて読めないので、
  CSS で `opacity: 1` と `-webkit-text-fill-color` を指定し直している
- `input` ハンドラの先頭にも `if (disabled) return;` を置いている（欄以外の経路対策）
- `updateMetaEditable()` は `syncMetaToInputs()` / `openDrawer()` / `closeDrawer()` から呼ぶ

### 廃止したもの（復活させないこと）

画面上部にあった **「既知点との誤差」バー**（`updateClosureDisplay`）と、
そこから開く**誤差モーダル**（`openErrorModal` / `exportErrorCsv` / `exportErrorExcel`）は
2026-09-01 に削除した。

- バーは**誤差が最大の測点1つ**を出すだけで、どの測点のことか画面から分からなかった
- 中身（測点名 / 既知GL / 測定GL / 誤差）は表の `基準高`・`誤差mm` 列に統合済み
- 誤差モーダルは入口がこのバーだけだったので、到達不能になるため一緒に削除した
- 誤差一覧の書き出しは **xlsx の「誤差一覧」シート**（`buildXlsxWorkbook`）に残っている。
  誤差だけの CSV 出力は無くなった

---

## 5. 数値の扱い

- 表示・保存とも **小数点3桁固定**。`fmt()` / `fmtInput()` が `toFixed(3)` する
- 入力時、**小数点以下4桁目は受け付けない**（`appendKey` の `decimalsOf(buffer) >= DECIMALS`）
- 小数点の二重入力も弾く
- 弾いたときは `rejectInput()` が
  **画面全体の赤い点滅**（`body.input-reject` / 約0.42秒）＋**ブザー**（190Hz square）＋**振動**で知らせる
- 表示は常に3桁だが、**編集中のセルだけは入力途中の値をそのまま見せる**（`cellDisplay()`）。
  打っている桁が見えなくなるのを避けるため

---

## 6. 入力パネル（キーパッド）

```
[ 測点名                    ]   [ 確認せず次へ ☐ ]  ← 入力中は OK / 修正
[ BS  1.235 （40px）        ]
[ 7 ] [ 8 ] [ 9 ]               [ AC  ]
[ 4 ] [ 5 ] [ 6 ]               [ C   ]
[ 1 ] [ 2 ] [ 3 ]               [ ⌫   ]
[ 0 ] [ . ]                     [ +/- ]
[   ] [   ] [ ↑ ]               [ ↓   ]
[   ] [   ] [ BS]               [ FS  ]
```

CSS は 4 列 6 行のグリッド。`0` と `.` は `grid-row: 4 / span 3` で**縦3行ぶんの高さ**を持ち、
右2列に `+/-`、`↑`/`↓`、`BS`/`FS` を積む。`AC` はキーパッドの4列目の先頭に移した。
各キーは `grid-column` / `grid-row` で明示配置している（`#prevRow` `#nextRow` `#modeBs` `#modeFs`）。

読み取り行と `AC` を載せる `.entry-top` も**キーパッドと同じ4列グリッド**にしてある
（読み取り行が `grid-column: 1 / span 3`、`AC` が4列目、`gap: 6px`）。
これで `AC` `C` `⌫` `+/-` の左右位置と幅が、`7 8 9` … `1 2 3` のブロックと完全に揃う。
`.entry-top` を独自の列指定にすると `AC` だけ幅がずれるので変えないこと。

### 廃止済みの機能（復活させないこと）

- **確定ボタン** — 廃止。測点名は測点名セルをタップして選択シートから入れる
- **`←` `→`（列移動ボタン）** — 廃止。列移動は物理キーボードの矢印/Tab のみ

### 上下移動（↑↓ボタン・上下矢印キー = `moveRow`）の制限

| フォーカス列 | 制限 |
|---|---|
| **FS** | 下へは「FS に数値を入れた最下段の**すぐ一つ下**」まで。空のままさらに下の行へは移動させない（`rejectInput()` で点滅＋ブザー） |
| **BS** | 移動先の行の FS が空で、かつ基準行でもない場合は、その行へ移したうえで**新しい基準高を求める**。選ばずに閉じたら元のセルにフォーカスが戻る |

### 新しい基準高を求める共通処理 `requestNewBaseGl(row, originCell)`

`moveRow` の BS 分岐と `chooseBs`（BS列から押したとき）が共有する。

1. `originCell` を `basePointSheetReturnTo` に控える
2. `selected` を対象行の BS に移して描画する
3. `openBasePointSheet(row, false)`（required ではない＝キャンセル可）を開く
4. `closeBasePointSheet()` が、基準高の決まらないまま閉じられたら `basePointSheetReturnTo` へ戻す

基準点を選んだ場合は `applyBasePointSelection` が行に `isBase: true` を立ててから閉じるので、
`hasBasePoint()` が真になり戻し処理は走らない。

### BS / FS ボタンの移動先

**BS ボタンが無効になる条件**（`isBsKeyBlocked()`）

| 状況 | 理由 |
|---|---|
| 入力の確認待ち（`isAwaitingConfirm()`） | 値が確定していない |
| **BS列**にいて、FS列に数値が1つも無い（`lastFilledFsRow() < 0`） | GL がどこにも無い |
| **FS列**にいて、その行の FS がまだ空 | この行の GL が決まっていない |

いずれも `IH = GL + BS` を出せない場面。意味のない BS 入力を防ぐためのもの。

FS を入れて FS ボタンで一つ下へ進むと、その行の FS は空なので BS ボタンは押せない。
数値を入れる（確認待ちなら OK を押す）と押せるようになる。

**FS ボタン**: `lastFilledFsRow() + 1` の行の FS。
行0は FS を持たないため `Math.max(..., 1)` で行1以降に限定している。

**BS ボタン**: 押したときのフォーカス列で行き先が変わる。

| 押したときのフォーカス | 行き先 | 基準高の要求 |
|---|---|---|
| **FS列** | **同じ行の BS** | しない |
| **BS列** | `lastFilledFsRow() + 1` の行の BS | **する** |
| その他 | `lastFilledFsRow() + 1` の行の BS | しない |

FS列から同じ行の BS へ行くのは、器高式では**同一行の FS と BS が同じ移器点**を指すため。
FS を入れた直後にその移器点の BS を続けて入力できる。

BS列から押したときの行き先は FS が空の行なので、器高を計算する元になる GL が無い。
そこで `requestNewBaseGl()` が新しい基準高を求める。選ばずに閉じたら元のセルへ戻る。

---

## 7. 入力値の確認（音声読み上げは廃止）

音声合成は処理が遅れて連打に追いつかないため、**読み上げは使わない**。
確認は「読み取り行に大きな文字で出す」ことと「OKボタンを挟む」ことで行う。
**打鍵ごとの読み上げにも、離脱時の読み上げにも戻さないこと。**

| タイミング | 動作 |
|---|---|
| テンキーをタップ | `clickTone()` — Web Audio の打鍵音（ピッ）だけ |
| 数値の入力を始めた | `markEntryStarted()` が `awaitingConfirm = true` にし、`OK` / `修正` が出る |
| 確認待ちのあいだ | **BS・FS ボタンが `disabled`**。`C` `⌫` `+/-` は有効のまま |
| `OK` を押した | 値を確定し確認待ちを解除。BS・FS が押せるようになる |
| `修正` を押した | 値を消して入力し直せる状態に戻す（確認待ちは続く） |

### 「確認せず次へ」チェック

`#skipConfirm` を押すと `skipConfirm` が反転する。

- **未チェック（初期値・橙色 ☐）**: 上表のとおり OK を挟む
- **チェック済み（水色 ☑）**: 確認を挟まず、入力後そのまま BS・FS を押せる

`skipConfirm` は保存しないので、読み込み直すと必ず未チェックに戻る。

### 表示

`.entry-readout` は上段に測点名、下段に列名＋値。
**値は 40px・太字**で、読み違えないようにしてある。小さくしないこと。

セルを移ったときは `awaitingConfirm = false` に戻す
（`chooseBs` / `chooseFs` / `moveRow` / `moveField` / `selectCell`）。

---

## 8. 基準点（必須フロー）

### 必須化

`ensureBasePoint()` が、行0に基準点（測点名＋標高）が無い表では
基準点シートを **required モード**で開き、決まるまで閉じられなくする。

呼ばれる場所: 新規現場のセットアップ完了時 / 表追加時 / 表切替時 / AC（全消去）後。

required モードでは
- `×` と「あとで設定する」を CSS で非表示（`#basePointSheet.required`）
- `closeBasePointSheet()` が案内を出して閉じるのを拒否
- ヒント文を赤字にし「設定するまで先へ進めません。」を付す

### 行0の GL・測点名は「選ぶ」もので「打つ」ものではない

行0は基準点の行。**GL と測点名は登録済みの基準点から選ぶ値**であり、
どの経路からも直接書き換えられてはいけない。
`isBasePointCell(row, field)` が `true` を返すセル（行0の `gl` と `point`）は

- キーパッドからの編集を全経路で禁止（`appendKey` / `backspace` / `toggleSign` / `clearBuffer`）
- `moveField` / `moveRow` の移動先にもならない（行0の編集可能列は `["bs"]` だけ）
- **タップすると基準点選択シートが開く**（未設定なら required モード）
- **読み取り行の測点名入力 `#activePoint` も `readOnly` にする**（2026-09-01 追加）

**行0で編集できるのは BS の数値だけ。**

#### `#activePoint`（読み取り行の測点名欄）の穴（2026-09-01 修正）

キーパッド経路は `isBasePointCell()` が塞いでいたが、
**読み取り行の測点名入力だけ素通しだった**。この欄は `input` イベントで
`commitPointName()` を呼び、`rows[selected.row].point` を直接書き換える。
行0の BS を選んでいる状態でもこの欄は編集でき、基準点名を壊せてしまった。
さらに `commitPointName()` には `locked` の判定も無く、**保護中でも書き換えられた**。

対策は3つ。**どれも外さないこと。**

| 場所 | 対策 |
|---|---|
| `updateReadout()` | 行0（`isBasePointCell(selected.row,"point")`）または `locked` のとき `readOnly` にし、`.readonly` クラスと「基準点から選択」プレースホルダを付ける |
| `commitPointName()` | 先頭で `locked` と `isBasePointCell()` を見て弾く（入力欄以外の経路対策） |
| `#activePoint` の `click` | 行0なら `openBasePointSheet()` を開き、選び直す場所へ案内する |

初期選択セルは行0の **BS**（旧仕様の GL から変更済み）。

---

## 9. その他の主要機能

| 機能 | 関数 | 備考 |
|---|---|---|
| 表の追加・削除・改名・切替 | `addTable` / `deleteTable` / `renameTable` / `switchTable` | 表ごとに `rows` を持つ |
| 保護（ロック） | `toggleLock` | `locked === true` で全入力を無効化。**既存データ読込時と表の切替時は必ずON** |
| 閉合差 | `closureForRow` / `computeClosureAll` | 登録済み基準点と同名の測点があれば差を mm 表示。±5mm未満=ok / ±10mm未満=warn / それ以上=error。**出す場所は表の `基準高`・`誤差mm` 列と、測点名の ▼ 展開、xlsx の「誤差一覧」シートだけ**（画面上部の誤差バーと誤差モーダルは 2026-09-01 に廃止） |
| Excel 出力 | `exportExcel` / `xlsxPackage` | 外部ライブラリ無しで OOXML(.xlsx) を自前生成し ZIP(store) で固める |
| 変位の集計 | `displacementSheetRows` / `displacementPoints` | 測点×測定回の変位表を作る。初期値は登録済み基準点、管理値は基本情報の `LIMIT` |
| 変位グラフの対象測点 | `isChartPoint` / `toggleChartPoint` | 登録済測点の「☑ 変位」で切り替える。未設定なら自動判定 |
| Excel / CSV 読込 | `importCsv` / `parseExcelXml` | 表シート先頭の `日付`/`時刻`/`作業名` から復元し、無ければシート名から復元する |
| 測点名選択シート | `openPointPicker` | 登録済み基準点からの選択と自由入力の両方に対応 |
| 確認・入力モーダル | `showConfirmModal` / `showInputModal` | `window.prompt` / `window.confirm` の置き換え |

---

## 9.4 保護（ロック）の扱い

`locked === true` のあいだは入力・移動が一切効かない。誤操作で既存の記録を書き換えないための保護。

**ONになるタイミング**

- 起動して「前回の続き」を選んだとき（`continueSavedWork`）
- Excel / CSV を読み込んだとき（`applyImportedWorkbook` / `applyImportedCsv`）
- **別の表に切り替えたとき（`switchTable`）** — 読み込んだ直後の表は必ず保護に戻す

**基準点チェックとの関係**

`ensureBasePoint()` は `locked` のあいだ何もしない。
表の切替時に保護がかかるため、基準点が未設定でもその場ではシートを出さない。
代わりに `toggleLock()` で**保護を解除した時点**（＝編集に入る時点）で `ensureBasePoint()` を呼ぶ。

---

## 9.5 画面サイズへの追従

420px 幅の1画面として設計してあるので、タブレットなど広い端末では
`applyUiScale()` が `document.body.style.zoom` で全体を拡大する。

- 拡大率 = `min(画面幅 / 420, 画面高さ × 0.5 / 入力パネルの高さ, 2.4)`
  入力パネルが画面の半分を超えないところで頭打ちにしている
- 高さで頭打ちになって幅が余ったぶんは `--app-max-width`（上限900px）として配り、
  表と入力パネルを横に広げて使い切る
- `zoom` は `vh` / `vw` に効かないので、拡大後の実寸を `--app-vh` / `--app-vw` として
  配り直している。**CSS で `100vh` / `100dvh` を直接書かないこと**
- 回転などに追従するため `resize` / `orientationchange` / `ResizeObserver` で呼び直す。
  `zoom` を変えると html の高さが変わって `ResizeObserver` が再発火するので、
  `lastUiViewport` で「表示領域が実際に変わったときだけ」処理する

| 端末 | 拡大率 | app幅 | パネル |
|---|---|---|---|
| 375x812（スマホ） | 1.00 | 100% | 53% |
| 768x1024（タブレット縦） | 1.19 | 100% | 50% |
| 1024x768（タブレット横） | 1.00 | 88%（900px上限） | 56% |

---

## 9.6 横向き（ランドスケープ）の2段組み

`(orientation: landscape) and (min-width: 900px)` のとき `.app` をグリッドにして2段組みにする。
**縦向きの見た目は一切変えていない。**

```
┌────────────────────┬─────────────────────┐
│ 現場名 / 作業名 / 日時│ 野帳表               │
│ 計算表の切替・保護    │ （縦をめいっぱい）     │
├────────────────────┤                     │
│ 情報パネル           ├─────────────────────┤
│ ・既知点との誤差      │ 読み取り行 ＋ 確認    │
│ ・登録済み基準点      │ テンキー             │
│ ・現在の状態         │ （押しやすいよう下端）  │
│ 保存済み            │                     │
└────────────────────┴─────────────────────┘
        38%                    残り
```

**テンキーは必ず右下**。表は右上。誤差や基準点の一覧は左にまとめる。

### グリッドの組み方

```css
grid-template-columns: minmax(300px, 1fr) var(--pad-max-width);
grid-template-rows: auto auto minmax(0, 1fr) auto auto;
grid-template-areas:
  "summary table"
  "switch  table"
  "info    table"
  "info    pad"
  "state   pad";
```

- `.side-stack { display: contents }` は縦横どちらでも有効。
  横向きでは `.entry-panel` と `.info-pane` がそのまま `.app` のグリッド項目になる
- `.entry-panel` は `position: static` にしてグリッドに乗せる（縦向きは `fixed` のまま）
- `.table-wrap` は `height: 100%; max-height: none; min-height: 0` で割り当て高さを使い切る
- `info` を3〜4行目にまたがせることで、左カラムの残りをすべて情報パネルに回している
- `.base-bar` と `#csvFile` は `display: none` なのでグリッド項目にならない

### 拡大率の決め方（`applyUiScale`）

| | 拡大率の上限 |
|---|---|
| 縦向き | `min(画面幅/420, 画面高さ×0.5/パネル高, 2.4)` |
| **横向き** | `min(画面高さ/(パネル高+300), 画面幅/980, 2.4)` |

横向きは**拡大しすぎると表の行数が減る**ので、テンキーの高さに加えて
表に 300px を残せるところで頭打ちにしている。

### 表とテンキーを同じ幅で右端に揃える（2026-09-01）

**テンキーは押しやすいよう必ず右下**。表もテンキーと同じ幅にして右端で揃える。

- 共通幅は `--pad-max-width`（既定 480px）。`.table-wrap` / `.entry-top` / `.keypad` の
  3つが `max-width: var(--pad-max-width); margin-left: auto; margin-right: 0`
- 親（`.app` と `.entry-panel`、横向きではグリッドの右カラム）の左右位置が同じなので、
  これだけで表・読み取り行・テンキーの左右の端がぴたりと合う
- 横向きは右カラムを `var(--pad-max-width)` 固定にし、**余った幅は左の情報パネルが受け取る**。
  表を右端へ寄せたぶん、左に無駄な空きを作らないため

### 表の列幅 —— `min()` に % を混ぜないこと（重要）

```css
.level-table th:nth-child(-n+4) { width: 52px; }   /* BS / IH / FS / GL */
```

**`width: min(110px, 17%)` のように % 混じりの `min()` で書くと、
`table-layout: fixed` の列では幅指定ごと無視され、全列が均等割りになる。**
以前これで4列が必要以上に広がっていた（2026-09-01 修正）。**必ず素の px で書くこと。**

数値は `-22.872` でも 27px なので、余白と罫線を含めて 52px あれば足りる。
余った幅は測点名列と誤差列が受け取る。

### 基準高・誤差の2列（2026-09-01 追加）

測点名の右に `基準高` と `誤差mm` の2列を置く。
どちらも**登録済み基準点と同名の測点にだけ**値が入る読み取り専用の列で、
`closureForRow(row)` の結果をそのまま出しているだけ。

| 列 | 中身 | 描画 |
|---|---|---|
| `基準高` | 登録済み基準点の既知GL（`closure.ref`）を3桁で | `refCell(closure)` |
| `誤差mm` | 実測GL − 既知GL を符号付きmmで。色は `closureClass()`（±5mm未満=緑 / ±10mm未満=橙 / それ以上=赤） | `diffCell(closure)` |

- **`fields` には入れない**。だから選択・列移動・キー入力の対象にならない
  （タップしても何も起きない）。`fields` に足すとキーパッドが計算列を壊す
- 誤差の値は `signedMm()` から ` mm` を除いた符号付き数値
- **スマホでは出さない**。列を増やす余地が無いため。
  `applyUiScale()` が `cssWidth >= UI_WIDE_COL_MIN`(520) のときだけ `body.show-wide` を付け、
  CSS がその有無で `.wide-col` の `display: none / table-cell` を切り替える
- 2列を出していないときは測点名列が最終列になるので、
  `body:not(.show-wide)` 側で測点名列の `border-right` を消している
- 展開行（測点名の ▼）の `colSpan` は **`visibleColumnCount()`**＝いま見えている列数。
  横向きは 7、スマホは 5。
  ▼の展開は「既知 / 測定 / 誤差」を1行で見せるもので、列とは役割が重なるが
  スマホでは列が出ないため残してある

> **`colSpan` を固定値にしないこと**（2026-09-01 の不具合）。
> 5列しか出していないのに `colSpan=7` にすると、ブラウザが**幻の列を2つ作る**。
> `table-layout: fixed` では幅がそこへ配られ、測点名列が 135px → 45px に縮んで
> **表の右側に空欄ができる**。幅の境界をまたいだときは `applyUiScale()` が
> 展開中の行を描き直して `colSpan` を作り直す。

### 左カラム＝ドロワーの中身を直接並べる（2026-09-01 改）

**横向き（PC・タブレット）ではアコーディオンのドロワーを使わない。**
画面に余裕があるので、ドロワーの中身をそのまま左カラム（`.info-pane`）に並べる。

| ブロック | 中身 | 元の場所 |
|---|---|---|
| 保存・読込 | 測定情報（作成日/作成時刻/現場名/作業名。**表示専用** → 第5.1章）＋ 保存 / 読込ボタン | ドロワーの `測点情報` |
| 基準点 | 基準点の追加（測点名＋数値）＋ 登録済測点の一覧（**削除できる**／各行に「☑ 変位」のチップ → 第4.2.2章） | ドロワーの `測点情報` / `登録済測点` |
| 現在の状態 | 基準点・器高IH・最新GL・記入行数（`renderInfoStatus`） | 元から情報パネル |

#### 廃止・統合したもの

| 元 | どうしたか |
|---|---|
| 情報パネルの「既知点との誤差」一覧 | **廃止**。表の `基準高` / `誤差mm` 列に統合した |
| 情報パネルの「登録済み基準点」（表示専用） | **廃止**。ドロワーの `#pointList`（削除できる方）に統合した |
| `renderInfoClosure()` / `renderInfoPoints()` | 削除。`renderInfoPane()` は `renderInfoStatus()` だけを呼ぶ |

#### 置き場所の切り替え（`placeDrawerContent`）

**要素を複製せず「同じノードを移動」している。** 複製すると入力値の同期ずれが起きるため。

```
#metaBar       ⇄ #slotMeta
#drawerActions ⇄ #slotActions
#pointEntry    ⇄ #slotPointEntry
#pointList     ⇄ #slotPointList
```

- 起動時と `applyUiScale()`（縦横が変わったとき）に呼ぶ。**ドロワーが開いている間は動かさない**
- 横向きに出しているあいだは `body.inline-panel` が付き、
  CSS が `.edge-open` / `#drawer` / `.drawer-backdrop` を隠す
- **`openDrawer()` は先頭で `placeDrawerContent(true)` を呼び、中身をドロワーへ戻す。**
  `setup` / `base` / `register` の各モードが `#savedPointName` などドロワー内の要素を
  使うため、これをしないとモードが機能しなくなる。`closeDrawer()` で左カラムへ戻す
- 戻すときは `DRAWER_SLOTS` の順に `appendChild` するので、元の並び順のまま復元される

縦向き（スマホ）は従来どおりドロワー＋アコーディオン。
縦向きには2段組みの左カラム自体が無いので、置き場所を作れないため。
**タブレットでも縦に持てばドロワーに戻る**（`orientation: landscape` の判定のため）。

左カラムの中身は `max-width: 620px` で頭打ち。大画面で入力欄が横に間延びしないようにしている。

### 実測

2026-09-01 の右端揃え後（デバイスpx）。表とテンキーは幅が一致し、左右の端が揃う。

| | 375×812 | 768×1024 | 1024×768 | 1366×1024 | 1920×1080 |
|---|---|---|---|---|---|
| 拡大率 | 1.00 | 1.19 | 1.045 | 1.39 | 1.50 |
| 表の幅 | 347 | 573 | 502 | 669 | 722 |
| テンキーの幅 | 355 | 573 | 502 | 669 | 722 |
| 左右の端のズレ | 右0 / 左8 | 1 | 0 | 0 | 0 |
| 数値1列 | 52 | 62 | 54 | 72 | 78 |
| 測点名列 | 135 | 162 | 140 | 193 | 208 |
| 基準高列 | なし | 74 | 65 | 86 | 93 |
| 誤差列 | なし | 69 | 61 | 81 | 87 |
| 情報パネル | — | — | 485 | 648 | 1145 |
| 見える行数 | 13 | 13 | 14 | 14 | 13 |

全サイズで横スクロールは発生せず、最下段まで到達でき、最下行の下罫線も出る。

375px でテンキーの左端が 8px ずれるのは `.app` の左パディングが 18px
（左端のドロワー取っ手のぶん）、`.entry-panel` が 10px だから。右端は揃っている。

**画面を大きくしても見える行数は増えない**（13〜14行）。
横向きの拡大率が「表に300px残す」ところで決まるため、画面が大きいほど拡大率も上がり、
CSSピクセル上の表の高さは常に約300pxになる。行数を増やしたいなら
`UI_LAND_TABLE_MIN` を上げるか、拡大率の式そのものを見直す必要がある。

---

## 10. 動作確認の手順

静的ファイルなので簡易サーバで開く。**Bash でサーバを起動しないこと。**
`.claude/launch.json` に設定を書き、preview ツールを使う。

`C:\Users\OWNER\開発\.claude\launch.json` に入っている実際の設定は次のとおり
（`--directory` はセッションの作業ディレクトリ `開発` からの相対パス）。

```json
{
  "name": "level",
  "runtimeExecutable": "python",
  "runtimeArgs": ["-m", "http.server", "8769", "--directory", "LEVEL"],
  "port": 8769
}
```

preview ツールに `level` を渡すと `http://localhost:8769/` で開く。

> preview ツールは**セッションの作業ディレクトリの** `.claude/launch.json` を読む。
> 対象フォルダ側ではないので注意。

### キャッシュで変更が反映されないとき

Service Worker とブラウザの HTTP キャッシュの二重キャッシュにより、
ファイルを書き換えても古い内容が返り続けることがある。**`navigate` を `force: true` で呼ぶのが確実**。
それでも駄目なら SW の登録解除 → `caches.delete()` → 強制リロード。

### コンソールからの検証

全関数がグローバルなので、`javascript_tool` から直接呼べる。
`window.speechSynthesis.speak` を差し替えると読み上げ内容を配列で捕捉でき、
音を鳴らさずに音声仕様を検証できる。

---

## 11. 既知の課題・申し送り

1. **複数PCで開発している** — 版管理は Dropbox の保管庫、コード本体は GitHub。
   **PCが変わるとユーザー名が変わるのでパスが合わなくなる**（旧: `mande` / 現: `OWNER`、
   Dropboxフォルダ名も `Dropbox` ではなく `Dropbox (個人用)`）。
   別PCで再開するときは第12章の手順で作業リポジトリを作り直すこと。
   **保管庫（Dropbox）をそのままコピーしても作業リポジトリにはならない** —
   保管庫に入っているのは `LEVEL_vrNNN` と `OLD` だけで、root に作業ファイルが無いため。
2. **改行コード** — `core.autocrlf=true` のため、作業ツリーは CRLF、
   スクリプトで書き換えたファイルは LF になる。内容差はないので放置してよい
3. **`selected` と `buffer` は必ず同じセルを指すこと**（2026-08-31 に起きた不具合）
   初期選択セルを行0のGLからBSへ変えたとき、`buffer = rows[0]?.gl` のままにした箇所が
   6つ残っていた。保存データを読み込むと BS欄に GL の値が表示され、
   そのままキーを押すと BS が壊れるところだった。
   `selected = { row, field }` を書いたら、直後の `buffer` が同じ `field` から
   取られているか必ず確認する。
4. **`navigator.vibrate` の警告** — プログラム経由のクリックではユーザー操作と見なされず
   コンソールに警告が出るが、実機の実タップでは正常に動作する

---

## 12. 別のPCで作業を再開する手順

保管庫（Dropbox）にあるのは `LEVEL_vrNNN` と `OLD` だけで、作業ファイルは root に無い。
**作業リポジトリは GitHub から作る。**

```bash
cd C:\Users\<ユーザー名>\開発
git clone https://github.com/kkodin/LEVEL.git
```

これで `開発\LEVEL` の直下に `app.js` `index.html` などが並び、そのまま改修に入れる。

| 場所 | 置くもの | git |
|---|---|---|
| `C:\Users\<ユーザー名>\開発\LEVEL` | 作業ファイル一式（root に直接） | **あり** |
| `C:\Users\<ユーザー名>\Dropbox (個人用)\Documents\GITHUB\LEVEL` | `LEVEL_vrNNN` と `OLD` のみ | **置かない** |

> **保管庫に `.git` を置かないこと。** Dropbox の同期が `.git` 内のファイルを掴み、
> `index.lock` 競合やリポジトリ破損を招く。
> （2026-09-01、保管庫直下に残っていた旧 `.git` は削除済み。
> 削除時に「Device or resource busy」が出る場合は PowerShell の `Remove-Item -Recurse -Force` を使うか、
> Dropbox を一時停止してから実行する）

作業リポジトリと保管庫の最新版が一致しているかは、次で確認できる。

```bash
diff -r 'C:\Users\<ユーザー名>\Dropbox (個人用)\Documents\GITHUB\LEVEL\LEVEL_vr014' . 
```

---

## 13. コミット履歴の要点

| コミット | 内容 |
|---|---|
| `4597134` | 変位グラフに出す測点を登録済測点のチェックで選べるようにする |
| `be651f0` | 変位測量むけに xlsx へ「変位グラフ」シートを自動生成する |
| `c948a96` | xlsx の表シートに 基準高・誤差mm 列と 日付/時刻/作業名 の3行を追加 |
| `6e23079` | スマホで測点名の▼を押すと表の右側に空欄ができる不具合を修正 |
| `757b96d` | 誤差バーを廃止し、測定情報を表示専用にする |
| `4d80b33` | 行0の測点名が読み取り行の入力欄から書き換えられる穴を塞ぐ |
| `62d2404` | 横向きはアコーディオンを廃止し、保存・読込を左カラムに直接置く |
| `49675cb` | 測点名の右に「基準高」列を追加 |
| `3e00ade` | 表とテンキーを同じ幅で右端に揃え、測点名の隣に誤差列を追加 |
| `acee0ab` | 新規現場で作られる表にも作成時刻の初期値（現在時刻）を入れる |
| `94618d6` | 横向きの配置を変更 — 左に情報、右に表とテンキー(下) |
| `b797854` | 横向きを2段組みにし、空いた領域に情報パネルを追加 |
| `519260f` | 別の表に切り替えたら保護状態に戻す |
| `06137bf` | 表に作成時刻(24時制 HH:MM)を追加し、日付と同じく編集できるようにした |
| `bf2d516` | 保存データ読込時にBS欄へGLの値が入る不具合 / 見出し行の固定を修正 |
| `ecaed8e` | キーパッド下2行を再配置（0と.を縦2行、↑↓をBS/FSの上へ） |
| `19e066d` | 上下移動の制限と、FSを消したときの計算結果クリア |
| `5b79fb6` | 引継ぎ用の仕様書.md を追加、版管理にOLDフォルダ運用を追記 |
| `36ab8f9` | 読み上げを「離脱操作2段階」の確認方式に変更 |
| `c60ae19` | 作業リポジトリを `開発\LEVEL` に移し CLAUDE.md 追加 |
| `dbd1e5b` | 打鍵音＋確認読み上げ方式に変更・基準点の設定を必須化 |
| `599995a` | キーパッド再配置・確定ボタン削除・小数点3桁の統一 |
| `59637be` | アプリアイコンを「Level / 野帳」2行デザインに変更 |
| `32bcf00` | 基準点入力画面の追加と Excel 出力の xlsx 化 |
